import { dev } from '$app/environment'

export const port = 5173
export const title = 'O1ON'
export const description = 'Personal finances, o(n)e click away'
export const url = dev ? `http://localhost:${port}` : 'https://o1on.com'
